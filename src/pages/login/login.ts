import Block from 'core/Block';

interface IProps {}

export class Login extends Block {
    constructor(props: IProps) {
        super({...props});
    }

    render(): any {
       return `
       sadasd
       `
    }
}